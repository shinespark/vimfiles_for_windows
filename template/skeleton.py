# coding: utf-8
import io
import numpy as np


def main():
    #  line_count = int(input())
    #  line_list = [input() for i in range(line_count)]
    #  line_list = [int(input()) for i in range(line_count)]
    #  line_count, x, y = map(int, input().split())
    #  line_list = [[int(i) for i in (input().split())] for i in range(line_count)]

    lines = '''
'''
    f = io.StringIO(lines)
    #  line_count = int(f.readline().strip())
    #  line_list = [f.readline().strip() for i in range(line_count)]
    #  line_list = [int(f.readline().strip()) for i in range(line_count)]
    #  line_count, x, y = map(int, f.readline().strip().split())
    #  line_list = [[int(i) for i in (f.readline().strip().split())] for i in range(line_count)]

    #  import pdb; pdb.set_trace()


if __name__ == "__main__":
    main()
